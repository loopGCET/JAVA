# Function

Function<T, R> represents a function that accepts one argument and produces a result.

```java
@FunctionalInterface
public interface Function<T, R> {
    R apply(T t);
}
```

For details, see: **[FunctionExample.java](FunctionExample.java)**

